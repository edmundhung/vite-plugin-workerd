{
  "name": "hello-world-example",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "pnpm --dir ../.. build",
    "prestart": "pnpm build",
    "start": "workerd serve dist/workerd.capnp",
    "dev": "vite dev",
    "build": "vite build",
    "test:runner": "vitest run",
    "test": "WORKERD_PORT=8788 start-server-and-test start tcp:127.0.0.1:8788 test:runner"
  },
  "dependencies": {
    "semver": "^7.7.2"
  },
  "devDependencies": {
    "@types/semver": "^7.7.0",
    "start-server-and-test": "^2.1.5",
    "vite": "^7.1.12",
    "vite-plugin-workerd": "workspace:*",
    "vitest": "^3.2.4",
    "workerd": "^1.20260410.1"
  }
}